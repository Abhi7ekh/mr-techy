function CSSSection() {
  return (
    <section id="css" className="tech-section">
      <h2>CSS</h2>
      <p>CSS is used for styling and layout of web applications.</p>
    </section>
  );
}

export default CSSSection;
